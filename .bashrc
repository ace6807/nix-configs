if [ -f ~/.bash_aliases ]; then
    source ~/.bash_aliases
fi

if [ -f ~/.cx_bash_aliases ]; then
    source ~/.cx_bash_aliases
fi

if [ -f ~/.bash_functions ]; then
    source ~/.bash_functions
fi

if [ -f ~/.cx_bash_functions ]; then
    source ~/.cx_bash_functions
fi
